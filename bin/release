#!/bin/sh

cat << EOF
---
default_process_types:
  web: "cd public && python -m SimpleHTTPServer \$PORT"
  web: "hugo server --baseURL=\$BASE_URL --port=\$PORT --appendPort=false --disableLiveReload=true"
EOF
